
This section describes the experimental setup, the measurement protocol used to isolate algorithm energy consumption, the implementation constraints across all six languages, the input sizes used for each algorithm, and idk.

\subsection{Experimental Setup}
All the experiments were conducted on a single dedicated machine to eliminate any inter-machine power variability. 
The Intel's RAPL interface exposes hardware energy counters directly to software, enabling fine-grained CPU package energy measurement without externam instruments \cite{10.1145/3177754}. 
Baseline idle power draw - 

\subsection{somehting...}
The main challenge in cross-language energy measurement is to ensure that we are measuring the energy consumed by the algorithm itself, and not the overhead of the language runtime or other background process. To address this, we designed a custom measurement harness that follows a strict protocol to isolate the algorithm execution. The harness uses a stdin/stout handshake design in which the program initializes once and then loops, executing the algorithm on each trigger without restarting the process.

The measurement sequence is shown in the Listing~\ref{lst:measurement_sequence}. 
\begin{lstlisting}[caption={Measurement sequence}, label={lst:measurement_sequence}, numbers=none]
    setup - not timed
    print "ready" - not timed
    harness newline - start timer
    run command - measurement
    print checksum - stop timer
    flush output - not timed
\end{lstlisting}

The flush() call on line 7 is critical. Without it, the newline sits in a kernel pipe buffer and the program never unblocks, causing the measurement to hang indefinitely. On the program side, each language blocks on its respective stdin/stdout primitive until the trigger arrives, executes the algorithm, and writes the checksum. Listing~\ref{lst:blocking_call} shows the blocking call used in each language.
\begin{lstlisting}[caption={Stdin primitives in each language implementation}, label={lst:blocking_call}, numbers=none]
    // C
    while (fgets(buf, sizeof(buf), stdin))  {run()}

    

\end{lstlisting}



fgets and sys.stdin and readline all blocks waiting for input until there is something to read from stdin/fgets. then reeads one line and returns it. So the harness will print "ready" and then wait for the next line. The next line is the signal to start the timer and run the command. After the command is done, the harness will print the checksum and stop the timer. This way we can measure the time taken by the command to run and also verify the output using checksum. 
For now, the command is "enter" btn, if I were to run the harness manually. But the verify file does this in the file itself.

\paragraph{Observation}
\begin{verbatim}
    - I can verify the thing now. 
    - The output that I get from the 
    baseline of C language can be 
    used to verify the time complexity 
    of the algorithms.
    - 
\end{verbatim}

\begin{table}[h]
    \centering
    \caption{C --- RUN \& VERIFY}\label{tab:c_run_verify}
    \small
    \begin{tabular}{lllll}
        \hline
        \textbf{Algorithm} & \textbf{Size} & \textbf{Output} & \textbf{Time} & \textbf{Status} \\
        \hline
            summation               & small    & 5050             &  0.0437ms  & PASS \\
            summation               & mid      & 50005000         &  0.0406ms  & PASS \\
            summation               & large    & 500000500000     &  0.4715ms  & PASS \\
            \texttt{binary\_search} & small    & 99               &  0.0448ms  & PASS \\
            \texttt{binary\_search} & mid      & 9999             &  0.0392ms  & PASS \\
            \texttt{binary\_search} & large    & 999999           &  0.0835ms  & PASS \\
            \texttt{merge\_sort}    & small    & 100              &  0.0428ms  & PASS \\
            \texttt{merge\_sort}    & mid      & 10000            &  0.2459ms  & PASS \\
            \texttt{merge\_sort}    & large    & 1000000          & 24.7405ms  & PASS \\
            bfs                     & small    & 100              &  0.0430ms  & PASS \\
            bfs                     & mid      & 1000             &  0.3039ms  & PASS \\
            bfs                     & large    & 5000             &  7.2774ms  & PASS \\
            \texttt{hash\_table}    & small    & 64850            &  0.0433ms  & PASS \\
            \texttt{hash\_table}    & mid      & 649985000        &  0.2467ms  & PASS \\
            \texttt{hash\_table}    & large    & 64999850000      &  2.1579ms  & PASS \\
            \texttt{matrix\_multiplication} & small    & 9175             &  0.0744ms  & PASS \\
            \texttt{matrix\_multiplication} & mid      & 646700           &  2.5604ms  & PASS \\
            \texttt{matrix\_multiplication} & large    & 10291750         & 47.1134ms  & PASS \\
        \hline
    \end{tabular}
    


    
\end{table}








baseline power draw - 40w 5s sample



50reps each sample




\subsubsection{Implementation-specific design decisions}
Each language's implementation follows the stdin/stdout handshake protocol. The program prints \texttt{"ready"} on startup, then blocks waiting for a newline trigger from the measurement harness before executing the algorithm. The specific I/O primitives used, and the rationale for each choice, are described below: 


\paragraph{C -}
    fgets

\paragraph{Python -}
    stdin/stdout

\paragraph{JavaScript -}
    readline
\paragraph{Java -}
    public static void main \\
    buffered reader/writer

\paragraph{Go -}
    bufio

\paragraph{Rust -}
    \texttt{wrapping\_mul}\,2654435761
